# Test fixture configuration - cyclic dependencies
structure:
  pattern: "{service}/{environment}/{region}/{module}"

execution:
  binary: terraform
  plan_enabled: true

extensions:
  gitlab:
    image:
      name: "hashicorp/terraform:1.6"
    stages_prefix: "deploy"
