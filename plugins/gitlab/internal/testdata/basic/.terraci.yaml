# Test fixture configuration - basic scenario
structure:
  pattern: "{service}/{environment}/{region}/{module}"

execution:
  binary: terraform
  init_enabled: true
  plan_enabled: true

extensions:
  gitlab:
    image:
      name: "hashicorp/terraform:1.6"
    stages_prefix: "deploy"
    auto_approve: false
    cache_enabled: true
