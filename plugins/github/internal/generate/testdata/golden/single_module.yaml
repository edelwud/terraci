# Generated by terraci — do not edit
name: Terraform
"on":
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
permissions:
    contents: read
    pull-requests: write
env:
    TERRAFORM_BINARY: terraform
jobs:
    apply-platform-stage-eu-central-1-vpc:
        runs-on: ubuntu-latest
        needs:
            - plan-platform-stage-eu-central-1-vpc
        environment: production
        concurrency:
            group: platform/stage/eu-central-1/vpc
            cancel-in-progress: false
        env:
            TF_ENVIRONMENT: stage
            TF_MODULE: vpc
            TF_MODULE_PATH: platform/stage/eu-central-1/vpc
            TF_REGION: eu-central-1
            TF_SERVICE: platform
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Download plan artifacts
              uses: actions/download-artifact@v4
              with:
                name: terraci-plan-platform-stage-eu-central-1-vpc
                path: .
            - name: Apply platform/stage/eu-central-1/vpc
              run: |-
                cd platform/stage/eu-central-1/vpc
                ${TERRAFORM_BINARY} init
                ${TERRAFORM_BINARY} apply plan.tfplan
    plan-platform-stage-eu-central-1-vpc:
        runs-on: ubuntu-latest
        concurrency:
            group: platform/stage/eu-central-1/vpc
            cancel-in-progress: false
        env:
            TF_ENVIRONMENT: stage
            TF_MODULE: vpc
            TF_MODULE_PATH: platform/stage/eu-central-1/vpc
            TF_REGION: eu-central-1
            TF_SERVICE: platform
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Plan platform/stage/eu-central-1/vpc
              run: |-
                cd platform/stage/eu-central-1/vpc
                ${TERRAFORM_BINARY} init
                ${TERRAFORM_BINARY} plan -out=plan.tfplan
            - name: Stage plan artifacts
              run: |-
                set -eu
                stage_dir='.terraci/artifacts/terraci-plan-platform-stage-eu-central-1-vpc/'
                rm -rf "$stage_dir"
                mkdir -p "$stage_dir"
                artifact_path='platform/stage/eu-central-1/vpc/plan.tfplan'
                if [ -e "$artifact_path" ]; then
                  artifact_dest="$stage_dir/$artifact_path"
                  mkdir -p "$(dirname "$artifact_dest")"
                  if [ -d "$artifact_path" ]; then cp -R "$artifact_path" "$artifact_dest"; else cp "$artifact_path" "$artifact_dest"; fi
                fi
                if ! find "$stage_dir" -type f | grep -q .; then
                  echo 'No artifact files staged for terraci-plan-platform-stage-eu-central-1-vpc'
                  exit 1
                fi
              if: always()
            - name: Upload plan artifacts
              uses: actions/upload-artifact@v4
              with:
                if-no-files-found: warn
                include-hidden-files: "true"
                name: terraci-plan-platform-stage-eu-central-1-vpc
                path: .terraci/artifacts/terraci-plan-platform-stage-eu-central-1-vpc/
                retention-days: "1"
              if: always()
