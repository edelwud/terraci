# Generated by terraci — do not edit
name: Terraform
"on":
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
permissions:
    contents: read
    pull-requests: write
env:
    TERRAFORM_BINARY: terraform
jobs:
    apply-platform-stage-eu-central-1-eks:
        runs-on: ubuntu-latest
        needs:
            - plan-platform-stage-eu-central-1-eks
            - apply-platform-stage-eu-central-1-vpc
        environment: production
        concurrency:
            group: platform/stage/eu-central-1/eks
            cancel-in-progress: false
        env:
            TF_ENVIRONMENT: stage
            TF_MODULE: eks
            TF_MODULE_PATH: platform/stage/eu-central-1/eks
            TF_REGION: eu-central-1
            TF_SERVICE: platform
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Download plan artifacts
              uses: actions/download-artifact@v4
              with:
                name: plan-platform-stage-eu-central-1-eks
            - name: Apply platform/stage/eu-central-1/eks
              run: |-
                cd platform/stage/eu-central-1/eks
                ${TERRAFORM_BINARY} init
                ${TERRAFORM_BINARY} apply plan.tfplan
    apply-platform-stage-eu-central-1-vpc:
        runs-on: ubuntu-latest
        needs:
            - plan-platform-stage-eu-central-1-vpc
        environment: production
        concurrency:
            group: platform/stage/eu-central-1/vpc
            cancel-in-progress: false
        env:
            TF_ENVIRONMENT: stage
            TF_MODULE: vpc
            TF_MODULE_PATH: platform/stage/eu-central-1/vpc
            TF_REGION: eu-central-1
            TF_SERVICE: platform
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Download plan artifacts
              uses: actions/download-artifact@v4
              with:
                name: plan-platform-stage-eu-central-1-vpc
            - name: Apply platform/stage/eu-central-1/vpc
              run: |-
                cd platform/stage/eu-central-1/vpc
                ${TERRAFORM_BINARY} init
                ${TERRAFORM_BINARY} apply plan.tfplan
    plan-platform-stage-eu-central-1-eks:
        runs-on: ubuntu-latest
        needs:
            - apply-platform-stage-eu-central-1-vpc
        concurrency:
            group: platform/stage/eu-central-1/eks
            cancel-in-progress: false
        env:
            TF_ENVIRONMENT: stage
            TF_MODULE: eks
            TF_MODULE_PATH: platform/stage/eu-central-1/eks
            TF_REGION: eu-central-1
            TF_SERVICE: platform
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Plan platform/stage/eu-central-1/eks
              run: |-
                cd platform/stage/eu-central-1/eks
                ${TERRAFORM_BINARY} init
                ${TERRAFORM_BINARY} plan -out=plan.tfplan
            - name: Upload plan artifacts
              uses: actions/upload-artifact@v4
              with:
                name: plan-platform-stage-eu-central-1-eks
                path: platform/stage/eu-central-1/eks/plan.tfplan
                retention-days: "1"
              if: always()
    plan-platform-stage-eu-central-1-vpc:
        runs-on: ubuntu-latest
        concurrency:
            group: platform/stage/eu-central-1/vpc
            cancel-in-progress: false
        env:
            TF_ENVIRONMENT: stage
            TF_MODULE: vpc
            TF_MODULE_PATH: platform/stage/eu-central-1/vpc
            TF_REGION: eu-central-1
            TF_SERVICE: platform
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Plan platform/stage/eu-central-1/vpc
              run: |-
                cd platform/stage/eu-central-1/vpc
                ${TERRAFORM_BINARY} init
                ${TERRAFORM_BINARY} plan -out=plan.tfplan
            - name: Upload plan artifacts
              uses: actions/upload-artifact@v4
              with:
                name: plan-platform-stage-eu-central-1-vpc
                path: platform/stage/eu-central-1/vpc/plan.tfplan
                retention-days: "1"
              if: always()
