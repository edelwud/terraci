Run linters and fix any issues found.

Execute `task lint` and analyze the output.

For each issue:
1. Read the file with the issue
2. Fix the issue following the project's coding style
3. Do NOT suppress lint warnings with nolint comments unless the warning is a false positive

After fixing, re-run `task lint` to confirm all issues are resolved.
