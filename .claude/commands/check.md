Run a quick verification cycle after code changes.

Execute `task check` and analyze the output. If there are errors:
1. Fix compilation errors first
2. Fix lint issues
3. Fix test failures

Report a brief summary of what passed and what was fixed.
