Build the project and run it with optional arguments.

If $ARGUMENTS is empty: execute `task build`.
If $ARGUMENTS is provided: execute `task dev:run -- $ARGUMENTS`.

Report build errors if any, and fix them.
